pipeline{
    agent { label 'nodo-windows' }

    stages {
        stage ('Configurar JDK'){
            steps{
                bat 'JAVA_HOME=C:\Program Files\Java\jdk-21'
            }
        }
	stage ('Checkout') {
            steps{
                git 'https://github.com/MaribellaMoreno/Selenium-Java-Web.git'
            }
        }

        stage ('Compilacion') {
            steps{
                bat 'mvn clean compile'
            }
        }
        stage ('Test Funcional Cucumber/Selenium') {
            steps{
                bat 'mvn clean install'
            }
        }
    }
}
